# Neow
A simple writting software inspired by Infinite canvas, few features with the intend to focus only in writing. Made using C# (WPF) with the extra of using Ollama locally (preconfigured prompts that can be changed). Available using the English language only.
